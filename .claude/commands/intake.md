---
description: 문진 답변 JSON을 받아 state/intake.json 으로 저장한다
---

아래 JSON 을 state/intake.json 으로 저장해라. 내용을 고치지 마라.
필수 필드(reviewer, doc_purpose)가 비어 있으면 저장하지 말고 알려줘라.
저장 후 곧바로 /redpen 절차를 시작해라.

$ARGUMENTS
