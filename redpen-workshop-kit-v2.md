# 빨간펜 (Red Pen) — 문진형
### 비개발자용 멀티에이전트 MVP 실습 키트 — 2시간 35분

---

## 0. 한 줄 요약

수강생이 **자료를 하나도 가져오지 않아도** 실습이 성립한다. 시스템이 12개 질문으로 물어보고, 수강생은 기억나는 만큼만 답한다. 그 답을 4개 에이전트가 받아 체크리스트를 만들고, 초안에 빨간줄을 긋고, 회의에서 나올 질문과 답변 초안까지 만들어 브라우저에 띄운다.

화면 맨 아래에 남는 것은 하나다. **"회의 전 준비 목록 3건, 예상 소요 40분."**

---

## 1. 이전 판에서 바뀐 것

| | 이전 (자료 수집형) | 이번 (문진형) |
|---|---|---|
| 입력 | 과거 코멘트 8건 이상 수집 | 12개 질문에 기억나는 만큼 답변 |
| 사전 과제 | 필수. 안 해오면 실습 불가 | **없음** |
| 근거 | 코멘트 원문 | 회상 + 빈도 응답 |
| 신뢰도 | 표본 수로 판정 | **항목별 신뢰등급 3단계로 화면에 표시** |
| 초안 | 있어야 검수 가능 | 없으면 **작성 가이드 모드**로 전환 |

운영 리스크를 설계로 없앴다. 대신 새 문제가 생긴다. **기억은 틀린다.** 그래서 이번 판의 핵심 장치는 신뢰등급이다. 어느 항목이 확실한 기억이고 어느 항목이 추정인지 화면에서 구분되지 않으면, 이 시스템은 그럴듯한 소설이 된다.

---

## 2. 먼저 정하고 갈 것 — 이건 사람 프로파일링이 아니다

상사의 성향을 학습하는 시스템으로 설계하면 두 가지가 동시에 망가진다. 윤리적으로 위험하고, 실용적으로도 안 맞는다. 성격 추정은 검증이 안 되고, 검증 안 되는 걸 근거로 문서를 고치면 매번 다르게 고치게 된다.

문진형에서는 이 위험이 더 커진다. 사람에게 직접 물어보면 대답에 감정이 섞이기 때문이다. 그래서 **문진표에 성격을 묻는 질문을 넣지 않는다.** 묻는 것은 전부 "무슨 말을 들었나", "몇 번 들었나", "무엇이 달랐나"다.

| 만드는 것 | 만들지 않는 것 |
|---|---|
| 반복 관찰된 **문서 요구사항 체크리스트** | 사람의 성격·성향·심리 프로파일 |
| 항목별 근거 질문 번호와 신뢰등급 | 답변에서 유추한 인물평 |
| 회상 빈도 (4회 이상 / 2~3회 / 1회) | "이런 사람은 이럴 것이다" 류의 일반화 |
| 문서를 어떻게 고칠지 | 대인 전략, 설득 화법 |

수강생에게 한 문장으로 말한다. **"우리가 학습하는 건 사람이 아니라, 그 사람이 문서에 반복해서 요구한 것이다."**

이 절을 수업 첫 10분에 배치한다. 문진을 시작한 뒤에 말하면 이미 머릿속에서 인물평이 만들어진 뒤다.

---

## 3. 문진표 설계 — 백지 회상은 실패한다

**"어떤 지적을 자주 받으셨나요?"** 라고 물으면 대부분 두세 개 쓰다 만다. 사람은 백지에서 못 떠올린다. 보기를 주면 떠올린다.

그래서 문진표의 중심은 **12개 보기 다중선택**이다. 자유 서술은 보조로만 쓴다. 이게 이 수업에서 가르치는 프롬프트 설계 원리이기도 하다. **에이전트에게 물을 때도 똑같다 — 열린 질문보다 선택지가 정확한 답을 뽑는다.**

### 문진표 12문항

**A. 대상 (2문항)**
1. 이 문서를 최종 승인하는 분은 누구인가요? (이니셜) — *필수*
2. 보통 어떻게 보고하나요? — 구두 회의 / 문서 회람 / 메신저 / 대면 1:1

**B. 반려 경험 (4문항) — 여기가 핵심**

3. 아래 말 중에, **실제로 들어본 것**을 모두 골라 주세요 *(다중선택)*
```
□ "이 숫자 어디서 나왔어요?"        □ "결론이 뭐예요?"
□ "대안은 없어요?"                  □ "너무 길어요"
□ "이거 누가 하는 거예요?"          □ "언제까지예요?"
□ "비용은요?"                       □ "리스크는요?"
□ "전에 했던 거랑 뭐가 달라요?"      □ "다른 팀이랑 얘기됐어요?"
□ "그래서 나한테 뭘 해달라는 거예요?" □ "이거 왜 지금이에요?"
```
4. 고른 항목마다 몇 번쯤 들었나요 — 4회 이상 / 2~3회 / 1회 / 기억 안 남
5. 위 목록에 없는 말인데, 자주 들은 게 있나요? *(자유, 3개까지)*
6. 반려됐을 때와 통과됐을 때, 문서가 뭐가 달랐나요? *(자유)*

**C. 통과 조건 (3문항)**

7. 한 번에 통과된 적이 있다면, 그때 문서에 뭐가 있었나요? *(자유)*
8. 그분이 문서를 열면 어디를 먼저 보시나요? — 첫 장 / 숫자 표 / 결론 / 일정 / 모름
9. 확실히 싫어하시는 게 있나요? *(자유, 없으면 비움)*

**D. 이번 문서 (3문항)**

10. 이번에 보고하려는 문서, 한 줄로 뭐 하는 건가요? — *필수*
11. 이번 문서에서 제일 자신 없는 부분은? *(자유)*
12. 초안이 있으면 붙여넣어 주세요 *(붙여넣기 또는 비움)*

**필수는 1번과 10번 둘뿐이다.** 나머지를 전부 건너뛰어도 시스템은 돈다. 대신 결과가 작아지고 신뢰등급이 전부 낮게 나온다. 그 상태를 화면이 정직하게 보여주고, **"정확도를 올리려면 3번과 7번에 답해 보세요"** 를 하단에 띄운다.

억지로 채우지 않는 게 이 시스템의 규칙이다. 문진도 예외가 아니다.

---

## 4. 신뢰등급 — 회상 데이터를 다루는 방법

각 체크리스트 항목에 점수를 매기고 등급을 붙인다. 계산은 규칙 기반이다. AI가 판단하지 않는다.

```
빈도 응답        4회 이상 = 3점 / 2~3회 = 2점 / 1회 = 1점 / 기억 안 남 = 0점
자유 서술(5,6번)에 같은 내용이 나오면        +1점
통과 조건(7번)과 반대 방향으로 일치하면      +1점

4점 이상 → high    "확실한 반복"
2~3점   → medium  "여러 번 들음"
1점     → low     "한 번 들음. 우연일 수 있음"
0점     → 제외하되 [회상 부족] 으로 남긴다
```

**화면에서 등급이 보이는 게 핵심이다.** high 항목만 굵게 표시하고, low 항목은 흐리게 처리한다. 수강생이 자기 기억의 확실한 부분과 흐릿한 부분을 눈으로 구분하는 순간이 이 수업에서 가장 조용하지만 강한 지점이다.

---

## 5. 두 가지 출력 모드

12번(초안)에 답했는지에 따라 갈린다.

```
[검수 모드]  초안 있음
   빨간줄 + 예상 질문 + 답변 초안 + 회의 전 준비 목록

[가이드 모드]  초안 없음
   작성 전 체크리스트 + 섹션별 필수 요소 + 예상 질문 + 회의 전 준비 목록
```

가이드 모드가 있기 때문에 준비물이 완전히 사라진다. 초안을 안 가져온 수강생도 마지막 화면을 자기 것으로 받는다.

---

## 6. 시스템 구조

```
web/intake.html   ← 12문항 폼. 답하고 "복사" 버튼
     │  (JSON 한 덩어리를 Claude Code에 붙여넣기)
     ▼
 오케스트레이터 (Claude Code / CLAUDE.md = 하네스 규칙)
     │
     ├─ ① 문진관   답변 구조화 → 체크리스트 + 신뢰등급 (로컬 MCP 계산)
     │      │
     │   ◆ HITL 게이트 — 사람이 체크리스트를 확인하고 승인해야 저장됨
     │      │         state/rubrics/{이니셜}.md  (기억)
     │      │
     ├─ ② 편집관   초안 있으면 빨간줄 / 없으면 작성 가이드  ┐
     ├─ ③ 질문관   예상 질문 + 근거 + 신뢰등급              ├─ 병렬
     ├─ ④ 답변관   답변 초안 + 없는 데이터 + 준비 시간      ┘
     │
     ▼
 state/review.json  →  web/index.html
```

### 위원 4명

| 위원 | 하는 일 | 절대 하지 않는 일 |
|---|---|---|
| ① 문진관 | 답변을 항목화, 신뢰등급 부여 | 답 안 한 문항을 추측으로 메우기 |
| ② 편집관 | 초안 위반 표시 / 작성 가이드 | 근거 항목 없는 지적, 문체 교정 |
| ③ 질문관 | 예상 질문 생성 | 문진 답변에 없는 질문 지어내기 |
| ④ 답변관 | 답변 초안 + 없는 데이터 표시 | 없는 숫자 만들어 넣기 |

제일 쉽게 나오는 실패는 **그럴듯한 예상 질문**이다. 근거 없이도 회의에서 나올 법한 질문은 얼마든지 생성된다. 그래서 모든 질문에 `basis`(문항 번호 또는 rubric_id)를 필수로 걸고, 근거를 못 대면 `speculative: true` 로 화면에서 흐리게 처리한다. 지우지는 않는다. **근거 있는 것과 없는 것이 구분되는 화면 자체가 학습이다.**

답변관의 `missing` 이 사람이 가장 오래 볼 자리다. 예상 질문 목록만 주면 읽고 끝이지만, "이 질문에 답하려면 3분기 이탈률 표가 필요하고 준비에 20분" 이 나오면 사람이 움직인다.

---

## 7. 하네스 6요소

| 요소 | 파일 | 수업에서 쓰는 말 |
|---|---|---|
| 규칙 | `CLAUDE.md` | "편집국 규정" |
| 역할 | `.claude/agents/*.md` | "위원 4명의 직무기술서" |
| 계약 | `schemas/*.json` | "제출 서식. 안 맞으면 반려" |
| 관측 | `.claude/settings.json` → `logs/trace.jsonl` | "회의록" |
| 게이트 | `state/approvals.json` + `.claude/gate.sh` | "결재란" |
| **기억** | `state/rubrics/*.md` | **"인수인계 파일"** |

여섯 단어만 쓴다. 용어를 더 늘리면 비개발자는 놓친다.

기억이 이 프로젝트의 특징이다. 다른 실습은 매번 새로 시작하지만, 빨간펜은 지난번 문진 결과를 파일로 들고 있다. **그리고 그 파일을 사람이 결재한다.** 자동으로 쌓이는 기억과 사람이 승인한 기억의 차이를 여기서 다룬다.

---

## 8. 타임라인 (155분)

| 시각 | 블록 | 하는 일 | 도달 상태 |
|---|---|---|---|
| 0:00–0:10 | 데모 + 선긋기 | 완성 화면 시연, 2절 합의 | 무엇을 만들고 무엇을 안 만드는지 |
| 0:10–0:28 | **문진** | `intake.html` 12문항 작성 → 복사 | 재료 확보 |
| 0:28–0:42 | 규칙·계약 | `CLAUDE.md` · 스키마 붙여넣기 | 하네스 뼈대 |
| 0:42–1:05 | 로컬 MCP | `redpen-desk` 생성, `/mcp` 확인 | **Aha 1** — 내 계산기를 AI가 부른다 |
| 1:05–1:10 | 휴식 | | |
| 1:10–1:30 | 문진관 + 게이트 | 체크리스트 생성, 근거 읽고 승인 | **Aha 2** — 내 기억이 신뢰등급 붙은 목록이 됨 |
| 1:30–1:52 | 검수 3인방 | 병렬 실행 | **Aha 3** — 내 초안에 빨간줄 / 작성 가이드 |
| 1:52–2:15 | 결과 FE 생성 | `web/index.html` → 브라우저 | **Aha 4** — 회의 전 준비 목록 |
| 2:15–2:28 | HOTL 전환 | 문진 답 3개 고쳐 무인 재실행 | 입력이 바뀌면 결과가 따라 바뀜 |
| 2:28–2:35 | 회고 | 검증 루프 예고, 하네스 6요소 복습 | |

**0:10–0:28 문진 블록은 빼면 안 된다.** 재료 없이 시작하면 강사 예제로 실습하게 되고, 마지막 준비 목록이 자기 것이 아니게 된다. Aha가 전부 죽는다.

시간이 밀리면 **2:15–2:28 HOTL 블록을 강사 시연으로 대체**한다. Aha 4까지는 어떤 경우에도 사수한다.

---

## 9. 사전 준비

### 수강생 준비물 — 없음

이전 판의 최대 리스크가 여기서 사라진다. 공지는 한 줄이면 된다.

```
【안내】 준비물 없습니다. 노트북만 가져오세요.
수업 중에 12개 질문에 답하시면 됩니다. 기억나는 만큼만 답하셔도 됩니다.
보고할 문서 초안이 있으면 가져오시면 더 좋습니다. 없어도 진행됩니다.
```

### 강사 준비물

- Claude Code 데스크탑 로그인 확인
- Python 3.10+ 및 `uv` 설치 (`curl -LsSf https://astral.sh/uv/install.sh | sh`)
- `jq` 설치 (macOS `brew install jq`)
- 실습 폴더 `redpen/` 배포 — **`web/intake.html` 는 완성본으로 포함**
- 단계별 스냅샷 `_snapshots/step1~step5`
- `chmod +x .claude/gate.sh` 를 배포 스크립트에 포함
- **샘플 초안 1건** — 초안을 안 가져온 수강생이 검수 모드도 한 번 보게 하려면 필요. R1·R2·R3 을 각각 일부러 위반하도록 만든다

`intake.html` 을 수강생이 만들지 않는다. 문진 폼은 도구고, 이 수업의 학습 대상이 아니다. 결과 화면(`index.html`)만 직접 만든다.

스냅샷이 생명줄이다. 비개발자 실습에서 한 명이 막히면 전체가 멈춘다. **"막히면 스냅샷 복사, 이유는 나중에"** 를 규칙으로 못 박고 시작한다.

---

## 10. 폴더 구조

```
redpen/
├─ CLAUDE.md
├─ .mcp.json
├─ web/
│  ├─ intake.html                  # 문진 폼 (배포본)
│  └─ index.html                   # 결과 화면 (수강생이 생성)
├─ state/
│  ├─ intake.json                  # 문진 답변
│  ├─ rubrics/KMS.md               # 기억
│  ├─ review.json
│  └─ approvals.json
├─ drafts/이번초안.md               # 있으면 검수, 없으면 가이드
├─ .claude/
│  ├─ settings.json
│  ├─ gate.sh
│  ├─ agents/
│  │  ├─ intaker.md                # ① 문진관
│  │  ├─ marker.md                 # ② 편집관
│  │  ├─ interrogator.md           # ③ 질문관
│  │  └─ responder.md              # ④ 답변관
│  └─ commands/redpen.md
├─ mcp_server/server.py
├─ schemas/review.schema.json
└─ logs/trace.jsonl
```

---

## 11. 복붙 자산

### 11-1. `CLAUDE.md`

```markdown
# 빨간펜 — 편집국 규정

## 목적
문서를 보고하기 전에, 반복될 지적을 미리 잡고 회의에서 나올 질문을 준비한다.

## 우리가 만드는 것
관찰된 문서 요구사항 체크리스트. 사람에 대한 프로파일이 아니다.
성격·성향·심리를 추정하는 문장을 어떤 파일에도 쓰지 않는다.
"꼼꼼한 편", "숫자에 민감", "완벽주의", "~한 스타일" 같은 표현을 금지한다.

## 입력
state/intake.json 이 유일한 재료다.
필수는 문항 1(리뷰어)과 문항 10(문서 목적) 둘뿐이다.
답하지 않은 문항은 그대로 비워둔다. 추측으로 채우지 않는다.

## 절차
1. 문진관(intaker)을 단독 실행한다.
2. 체크리스트 초안을 사용자에게 보여준다. 항목마다 근거 문항 번호와 신뢰등급을 함께 보여준다.
3. 승인을 받은 뒤에만 save_rubric 을 호출한다.
4. 승인 후 편집관(marker)·질문관(interrogator)·답변관(responder)을 동시에 실행한다.
5. 결과를 종합해 save_review 를 호출한다.

## 모드
- state/intake.json 의 문항 12에 초안이 있으면 검수 모드. drafts/ 에 저장하고 채점한다.
- 없으면 가이드 모드. 작성 전 체크리스트와 섹션별 필수 요소를 만든다.
  가이드 모드에서 초안을 대신 써주지 않는다. 무엇을 어디에 써야 하는지만 알려준다.

## 근거 규칙
- 모든 체크리스트 항목에 근거 문항 번호와 신뢰등급(high/medium/low)을 단다.
- 신뢰등급은 weight_rubric 이 계산한 값을 그대로 쓴다. 직접 판단하지 않는다.
- 모든 지적에 rubric_id 를 단다. 체크리스트에 없는 걸 지적하지 않는다.
- 모든 예상 질문에 근거를 단다. 근거를 못 대면 speculative: true 로 표시한다. 지우지 않는다.
- 초안에 없는 숫자·사실·인과를 답변에 만들어 넣지 않는다.
  근거가 없으면 answer 를 짧게 쓰고 missing 에 적는다.

## 개인정보 규칙
- 사람 이름은 이니셜로만 쓴다.
- 회사·제품·프로젝트 고유명사는 저장물에서 일반명사로 바꾼다.

## 금지
- 게이트 승인을 스스로 true 로 바꾸지 않는다.
- 자동 판정 항목을 직접 세지 않는다. 반드시 score_draft 를 호출한다.
- state/ 와 logs/ 밖의 파일을 삭제하지 않는다.

## 중단 규칙
- 같은 에이전트가 3회 실패하면 멈추고 사람을 호출한다.
- 체크리스트 항목이 2개 이하면 부족한 상태 그대로 보고하고,
  어느 문항에 답하면 좋아지는지 알려준다. 채워 넣지 않는다.
- high 등급 항목이 하나도 없으면 그 사실을 화면 상단에 표시하도록 review.json 에 기록한다.

## 말투
한국어. 단정적으로. 근거 없는 문장은 쓰지 않는다.
```

### 11-2. 문진 결과 — `state/intake.json`

`intake.html` 의 복사 버튼이 만들어 주는 형태다. 수강생이 직접 쓰지 않는다.

```json
{
  "reviewer": "KMS",
  "report_form": "구두 회의",
  "heard": [
    { "q": "이 숫자 어디서 나왔어요?", "freq": "4회 이상" },
    { "q": "결론이 뭐예요?",           "freq": "2~3회" },
    { "q": "대안은 없어요?",           "freq": "2~3회" },
    { "q": "언제까지예요?",            "freq": "1회" }
  ],
  "heard_free": ["집계 기간을 자주 물어봄"],
  "diff_rejected_vs_passed": "통과한 건 첫 장에 결론이 있었음",
  "passed_why": "표 아래에 출처를 다 달았을 때 한 번에 통과",
  "reads_first": "숫자 표",
  "dislikes": "장표가 5장 넘어가는 것",
  "doc_purpose": "3분기 이탈률 개선안 승인 요청",
  "my_worry": "비용 근거가 약함",
  "draft": "없음",
  "skipped": [9]
}
```

`skipped` 배열이 중요하다. **답을 안 한 것과 답이 없는 것은 다르다.** 문진관이 이걸 구분해야 "정확도를 올리려면 어느 문항에 답하라"를 말할 수 있다.

### 11-3. 기억 — `state/rubrics/KMS.md` (문진관 산출물 예시)

```markdown
---
reviewer: KMS
source: intake
answered: 11
skipped: [9]
high_count: 1
updated: 2026-08-26
approved_by_human: true
---

## R1. 숫자에 출처·기간·집계 기준  [high · 5점]
- 근거: 문항3에서 «이 숫자 어디서 나왔어요?» 을 4회 이상 들음 (3점)
        문항5·6 답변에도 같은 내용이 있음 (+1)
        문항7 통과 이유에도 관련 내용이 있음 (+1)
- 대응: 표 아래에 "출처 / 기간 / 집계 기준" 세 가지를 적는다
- 판정: 자동 (숫자 포함 문장 중 출처 표기 비율)

## R2. 첫 화면의 결론  [medium · 3점]
- 근거: 문항3에서 «결론이 뭐예요?» 을 2~3회 들음 (2점)
        문항5·6 답변에도 같은 내용이 있음 (+1)
- 대응: 첫 10줄 안에 결론 한 줄과 핵심 숫자를 둔다
- 판정: 자동 (첫 10줄 내 결론 키워드 유무)

## R3. 대안 2개 이상  [medium · 2점]
- 근거: 문항3에서 «대안은 없어요?» 을 2~3회 들음 (2점)
- 대응: 고른 안 + 검토했다가 버린 안 1개를 같이 쓴다
- 판정: 자동 (대안 섹션 항목 수)

## R6. 완료 시점  [low · 1점]
- 근거: 문항3에서 «언제까지예요?» 을 1회 들음 (1점)
- 우연일 수 있음. 다음 보고 후 재확인 필요
- 판정: 수동

## [회상 부족] 문서 길이
- 문항9 "장표 5장 넘어가는 것" 이 있으나 문항3에서 "너무 길어요" 를 고르지 않음
- 점수 0. 패턴으로 보지 않음. 다음 문진에서 재확인
```

`[회상 부족]` 섹션을 지우지 않고 남긴다. 다음번 문진에서 승격되는 구조이고, **답변이 늘면 결과가 달라진다는 걸 수강생이 눈으로 본다.**

### 11-4. 계약 — `schemas/review.schema.json`

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "빨간펜 검수 결과",
  "type": "object",
  "required": ["mode", "reviewer", "evidence", "rubric", "marks", "questions", "total_prep_minutes"],
  "properties": {
    "mode":     { "type": "string", "enum": ["review", "guide"] },
    "reviewer": { "type": "string", "description": "이니셜만" },
    "evidence": {
      "type": "object",
      "required": ["answered", "skipped", "high_count"],
      "properties": {
        "answered":   { "type": "integer", "description": "답한 문항 수 (12 중)" },
        "skipped":    { "type": "array", "items": { "type": "integer" } },
        "high_count": { "type": "integer" },
        "improve_by": { "type": "array", "items": { "type": "integer" },
                        "description": "여기에 답하면 정확도가 오르는 문항 번호" }
      }
    },
    "rubric": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title", "confidence", "score", "basis", "action"],
        "properties": {
          "id":         { "type": "string" },
          "title":      { "type": "string" },
          "confidence": { "type": "string", "enum": ["high", "medium", "low"] },
          "score":      { "type": "integer" },
          "basis":      { "type": "array", "items": { "type": "string" },
                          "description": "문항 번호와 응답. 최소 1개" },
          "action":     { "type": "string", "description": "문서를 어떻게 고칠지. 한 줄" }
        }
      }
    },
    "marks": {
      "type": "array",
      "description": "review 모드에서만 채움",
      "items": {
        "type": "object",
        "required": ["rubric_id", "line", "issue", "fix"],
        "properties": {
          "rubric_id": { "type": "string" },
          "line":      { "type": ["integer", "array"] },
          "issue":     { "type": "string" },
          "fix":       { "type": "string" }
        }
      }
    },
    "guide": {
      "type": "array",
      "description": "guide 모드에서만 채움",
      "items": {
        "type": "object",
        "required": ["section", "must_include", "rubric_id"],
        "properties": {
          "section":      { "type": "string" },
          "must_include": { "type": "array", "items": { "type": "string" } },
          "rubric_id":    { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "questions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["q", "basis", "confidence", "speculative", "answer", "missing", "prep_minutes"],
        "properties": {
          "q":            { "type": "string", "description": "실제로 들을 법한 구어체" },
          "basis":        { "type": "array", "items": { "type": "string" },
                            "description": "rubric_id 또는 문항 번호. 비면 speculative 여야 함" },
          "confidence":   { "type": "string", "enum": ["high", "medium", "low"] },
          "speculative":  { "type": "boolean" },
          "answer":       { "type": "string" },
          "missing":      { "type": "array", "items": { "type": "string" } },
          "prep_minutes": { "type": ["number", "string"] }
        }
      }
    },
    "total_prep_minutes": { "type": "number" },
    "halts": { "type": "array", "items": { "type": "string" } }
  }
}
```

> 수업 포인트: `basis` 가 비었는데 `speculative` 가 false 면 스키마는 통과하지만 규칙 위반이다. 그래서 `save_review` 도구가 이걸 따로 검사한다. **계약이 잡는 것과 도구가 잡는 것이 다르다**를 여기서 짚는다.

### 11-5. 로컬 MCP — Claude Code에 붙여넣을 프롬프트

```
mcp_server/server.py 에 FastMCP 기반 stdio MCP 서버를 만들어줘. 이름은 redpen-desk.
도구 4개만 만든다. 읽기 2개, 쓰기 2개.

[읽기]
1) weight_rubric() -> str
   state/intake.json 을 읽어 체크리스트 항목별 점수와 신뢰등급을 규칙대로 계산한다.
   판단하지 않는다. 아래 규칙 그대로만 적용한다.
   - 문항3에서 선택된 항목마다 빈도 점수: "4회 이상"=3, "2~3회"=2, "1회"=1, "기억 안 남"=0
   - 문항5(자유서술) 또는 문항6에 같은 취지의 내용이 있으면 +1
   - 문항7(통과 조건)과 반대 방향으로 일치하면 +1
   - 문항9(싫어하는 것)만 있고 문항3 선택이 없으면 점수 0으로 두고 [회상 부족]으로 표시
   - 등급: 4점 이상 high / 2~3점 medium / 1점 low / 0점 제외
   반환: 항목별 {id, title, score, confidence, basis[]}
        + answered(답한 문항 수), skipped[], high_count
        + improve_by: 답하면 등급이 오를 가능성이 있는 문항 번호 배열
   문항 1과 10이 비어 있으면 계산하지 말고 "필수 문항 누락" 을 반환한다.

2) score_draft(draft_path: str) -> str
   drafts/ 의 마크다운 초안을 규칙 기반으로만 채점한다.
   - conclusion_in_first_10_lines: 첫 10줄에 숫자와 결론 키워드(결론|제안|권고|요청)가 있나
   - sourced_ratio: 숫자가 들어간 문장 중, 같은 문장이나 인접 각주에
                    출처 표기(출처|기준|집계|기간|월|분기)가 있는 비율
   - option_count: "대안" 또는 "안 1 / 안 2" 패턴으로 잡히는 항목 수
   - hedge_count: 미결 표현(검토 필요|추후|지속적으로|모니터링|향후) 등장 횟수
   - avg_para_chars: 문단 평균 글자 수
   반환: 지표값 + 위반 위치의 줄 번호 배열.
   파일이 없으면 {"mode":"guide"} 를 반환한다. 초안을 만들어내지 않는다.
   초안 본문은 반환하지 않는다. 지표와 줄 번호만 반환한다.

[쓰기]
3) save_rubric(reviewer: str, payload: str) -> str
   state/rubrics/{reviewer}.md 로 저장. 저장 전 검증하고 실패 시 저장하지 않는다.
   - 모든 항목에 근거 문항 번호가 1개 이상 있는가
   - confidence 값이 weight_rubric 계산 결과와 일치하는가
   - 점수 0인 항목이 [회상 부족] 으로 표시돼 있는가
   - 금지 표현(꼼꼼|민감|성향|성격|완벽주의|스타일) 이 본문에 없는가

4) save_review(payload: str) -> str
   payload 를 schemas/review.schema.json 으로 검증한 뒤 state/review.json 에 저장.
   추가 검증: basis 가 빈 배열인데 speculative 가 false 인 질문이 있으면 저장 거부.
   실패 시 어떤 필드가 틀렸는지 반환한다.

프로젝트 루트에 .mcp.json 을 만들어 이 서버를 등록해줘.
마지막에 서버가 실제로 뜨는지 실행해서 확인하고, 안 되면 고쳐줘.
```

수강생은 코드를 쓰지 않는다. **요구사항을 말로 정의하는 것이 이 블록의 학습 목표**다.

**소크라틱 진행용 질문 4개**

- "신뢰등급을 왜 AI가 아니라 함수가 정하나?" → 같은 답변에 같은 등급이 나와야 한다. 매번 다르면 뭘 믿을지 알 수 없다
- "왜 초안 본문을 반환하지 않고 줄 번호만 주나?" → 문서 전체가 매 호출마다 대화에 들어가면 비용이 늘고, 에이전트가 원문 문체에 끌려간다. **필요한 최소한만 넘기는 게 하네스다**
- "`save_rubric` 이 금지 표현을 검사하는 이유는?" → 규칙은 프롬프트에도 썼다. 그래도 코드로 한 번 더 막는다. 프롬프트는 확률이고 검증은 결정이다
- "도구를 4개로 끊은 이유는?" → 도구가 많으면 에이전트가 뭘 부를지 헷갈린다. 읽기 2·쓰기 2로 대칭을 맞췄다

참고용 `.mcp.json` (막힐 때만 배포):
```json
{
  "mcpServers": {
    "redpen-desk": {
      "command": "uv",
      "args": ["run", "--with", "mcp[cli]", "python", "mcp_server/server.py"]
    }
  }
}
```

연결 확인은 `/mcp` → `redpen-desk` 가 뜨면 **Aha 1**.

### 11-6. 서브에이전트 — `.claude/agents/`

**`intaker.md`**
```markdown
---
name: intaker
description: 문진 답변을 체크리스트로 구조화하고 신뢰등급을 붙인다. 가장 먼저 단독 실행한다.
tools: mcp__redpen-desk__weight_rubric, mcp__redpen-desk__save_rubric
---

너는 문진관이다. 사람을 분석하지 않는다. 답변을 항목화한다.

절차:
1. weight_rubric 을 호출한다. 점수와 등급은 반환값을 그대로 쓴다. 직접 매기지 않는다.
2. 각 항목을 체크리스트로 쓴다. 항목마다 아래를 채운다.
   - 제목
   - 신뢰등급과 점수
   - 근거: 문항 번호와 응답 내용. 최소 1개
   - 대응: 문서를 어떻게 고치면 되는지. 한 줄
   - 판정: 자동(규칙으로 검사 가능) 또는 수동
3. 점수 0인 항목은 [회상 부족] 을 붙이고 그대로 남긴다. 지우지 않는다.
4. 사용자에게 보여줄 때 improve_by 문항 번호와 그 문항의 질문 문구를 함께 알려준다.
5. 승인을 받은 뒤 save_rubric 을 호출한다.

금지:
- 답하지 않은 문항의 내용을 추측해 채우지 않는다.
- 답변에서 사람의 성격·성향·심리를 추정하지 않는다.
  "꼼꼼한 편", "숫자에 민감", "완벽주의", "~한 스타일" 같은 표현을 쓰지 않는다.
- 항목 수를 채우려고 억지 분류를 만들지 않는다. 3개면 3개로 보고한다.
- 등급을 올려서 보고하지 않는다. low 는 low 로 보여준다.
```

**`marker.md`**
```markdown
---
name: marker
description: 초안이 있으면 체크리스트로 검수하고, 없으면 작성 가이드를 만든다.
tools: Read, mcp__redpen-desk__score_draft
---

너는 편집관이다. score_draft 를 먼저 호출해 어느 모드인지 확인한다.

[검수 모드]
- 자동 판정 항목은 반드시 score_draft 결과를 쓴다. 직접 세지 않는다.
- 모든 지적에 rubric_id 를 단다. 체크리스트에 없는 걸 지적하지 않는다.
- 지적은 [줄 번호] + [무엇이 빠졌는지] + [어떻게 고칠지] 순으로 쓴다.
- 문장이 어색하다는 이유로 지적하지 않는다. 문체 교정은 이 위원의 일이 아니다.
- 위반이 없으면 없다고 쓴다. 만들어내지 않는다.

[가이드 모드]
- 문항10(문서 목적)을 기준으로 섹션 구성을 제안한다.
- 각 섹션에 어떤 요소가 반드시 들어가야 하는지 쓰고, 근거 rubric_id 를 단다.
- 초안을 대신 써주지 않는다. 무엇을 어디에 써야 하는지만 알려준다.
- 체크리스트에 근거가 없는 섹션은 제안하지 않는다.
```

**`interrogator.md`**
```markdown
---
name: interrogator
description: 이 문서로 보고했을 때 나올 질문을 예측한다.
tools: Read
---

너는 질문관이다.

규칙:
- 질문마다 rubric_id 또는 문항 번호를 basis 에 단다.
- 질문의 confidence 는 근거가 된 rubric 항목의 등급을 그대로 따른다.
- 근거를 못 대는 질문은 speculative: true 로 표시한다. 지우지는 않는다.
- 질문은 실제로 들을 법한 구어체로 쓴다.
  "~에 대한 근거는 무엇입니까" 가 아니라 문진 문항3의 말투를 따른다.
- 문항11(사용자가 불안해하는 지점)은 질문 후보로 우선 검토한다.
  본인이 약하다고 느낀 곳은 실제로 질문이 나온다.
- 질문 개수를 채우지 않는다. 근거 있는 게 3개면 3개다.
```

**`responder.md`**
```markdown
---
name: responder
description: 예상 질문에 대한 답변 초안을 쓰고, 답하려면 필요한데 지금 없는 데이터를 짚는다.
tools: Read
---

너는 답변관이다. 검색 도구가 없다.

각 질문에 대해 셋을 쓴다.
- answer: 초안이나 문진 답변에 있는 내용만으로 쓸 수 있는 답변. 30초 안에 말할 분량
- missing: 이 답변을 제대로 하려면 필요한데 지금 없는 것. 없으면 빈 배열
- prep_minutes: missing 을 준비하는 데 걸릴 대략 시간

가이드 모드(초안 없음)에서는 answer 를 "아직 쓸 수 없음" 으로 두고
missing 에 무엇을 준비해야 답할 수 있는지 적는다. 이때 answer 를 지어내지 않는다.

금지:
- 초안에 없는 숫자·사실·인과를 만들어 넣지 않는다.
- "검토하겠습니다", "확인 후 말씀드리겠습니다" 를 기본 답변으로 쓰지 않는다.
  실제로 그게 유일한 답이면 missing 에 무엇을 확인해야 하는지 적는다.
```

> **답변관에게 검색 도구를 주지 않는다.** 검색으로 근거를 보강하기 시작하면 `missing` 이 비어버리고, 이 시스템의 결론인 준비 목록이 사라진다. 역할마다 도구 권한을 다르게 주는 것이 하네스라고 설명한다.

### 11-7. 슬래시 명령 — `.claude/commands/redpen.md`

```markdown
---
description: 문진 결과로 체크리스트를 만들고 문서를 검수한다
---

state/intake.json 을 재료로 CLAUDE.md 의 절차를 그대로 따라라.
체크리스트를 보여줄 때 항목마다 근거 문항의 원래 질문과 사용자의 답변을 함께 보여줘라.
사용자가 자기 답변을 다시 읽고 판단할 수 있어야 한다.
각 단계가 끝날 때마다 지금 어느 위원이 일하고 있는지 한 줄로 보고해라.

추가 지시: $ARGUMENTS
```

---

## 12. 문진 폼 — `web/intake.html` (강사 배포본)

수강생이 만들지 않는다. 생성이 필요하면 아래 프롬프트를 쓴다.

```
web/intake.html 을 만들어줘. 의존성 없는 단일 HTML 파일이다. 저장 기능은 쓰지 않는다.

12문항 폼을 만든다. (문항 내용은 3절 참조)
- 문항1(리뷰어 이니셜)과 문항10(문서 목적)은 필수. 나머지는 전부 건너뛸 수 있다
- 문항3은 12개 보기 체크박스. 체크한 항목 아래에만 빈도 선택(4회 이상/2~3회/1회/기억 안 남)이 펼쳐진다
- 문항12(초안)는 큰 textarea. 비워두면 "없음" 으로 처리
- 상단에 진행 표시: "3 / 12 답변함". 필수 2개가 비면 복사 버튼을 비활성화

하단에 큰 버튼 하나: [답변 복사하기]
- 누르면 state/intake.json 형식의 JSON 한 덩어리를 클립보드에 복사한다
- 답 안 한 문항 번호는 skipped 배열에 넣는다. 빈 문자열로 넣지 않는다
- 복사 후 안내: "Claude Code 에 붙여넣고 /redpen 을 실행하세요"

스타일: 무채색, 시스템 폰트, 1px 괘선. 화면 폭 720px 중앙 정렬.
한 화면에 다 보이게 하지 말고, 섹션 A~D 를 세로로 스크롤되게 둔다.
```

> **답 안 한 문항을 빈 문자열로 넣지 않고 `skipped` 배열로 분리하는 게 이 폼의 유일한 기술적 요구다.** 빈 문자열로 넘기면 "답이 없다"와 "안 물어봤다"가 섞이고, 문진관이 improve_by 를 계산할 수 없다. 강사가 이 지점을 짚어주면 좋다.

---

## 13. 관측 — `.claude/settings.json`

```json
{
  "hooks": {
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "jq -c '{ts: now|todate, ev: \"start\", agent: .agent_type}' >> logs/trace.jsonl",
            "async": true
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "jq -c '{ts: now|todate, ev: \"stop\", agent: .agent_type}' >> logs/trace.jsonl",
            "async": true
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "mcp__redpen-desk__save_rubric",
        "hooks": [
          { "type": "command", "command": "bash .claude/gate.sh" }
        ]
      }
    ]
  }
}
```

---

## 14. 게이트 — 어디에 거는지가 논점이다

**`.claude/gate.sh`**
```bash
#!/usr/bin/env bash
APPROVED=$(jq -r '.rubric_approved // false' state/approvals.json 2>/dev/null)
if [ "$APPROVED" = "true" ]; then
  exit 0
fi
echo "[게이트] 체크리스트는 사람이 확인해야 저장됩니다. 항목별 근거 문항과 신뢰등급을 직접 읽어보고, state/approvals.json 의 rubric_approved 를 true 로 바꾸세요." >&2
exit 2
```

**`state/approvals.json`**
```json
{ "rubric_approved": false, "mode": "HITL" }
```

### 진행 순서

1. 승인 안 한 상태로 문진관을 끝까지 돌린다 → **훅이 exit 2로 막고, AI가 "승인이 없어 저장 못 했다"고 보고한다**
2. 수강생이 항목별 근거 문항과 자기 답변을 직접 읽는다
3. `false` → `true` 로 한 글자 고친다
4. 다시 시도 → 저장된다

### 던질 질문 세 개

**"AI에게 승인받고 저장하라고 부탁한 게 아닙니다. 코드가 막았습니다. 뭐가 다를까요?"**
→ 부탁은 확률이고, 훅은 결정이다.

**"왜 체크리스트 저장에는 결재가 필요하고, 검수 결과 저장에는 없을까요?"**
→ 체크리스트는 한 번 틀리면 **그 뒤 모든 검수가 같은 방향으로 틀린다.** 검수 결과는 틀려도 그 문서 하나로 끝난다. 게이트는 아무 데나 거는 게 아니라 **되돌리기 비용이 큰 지점**에 건다.

**"문진형에서 이 게이트가 더 중요해진 이유는?"**
→ 이전 판은 실제 코멘트 원문이 근거였다. 지금은 **기억이 근거다.** 사람이 자기 기억을 한 번 다시 읽고 넘어가는 절차가 없으면, 잘못 기억한 것이 그대로 체크리스트가 되어 계속 남는다.

`chmod +x .claude/gate.sh` 를 잊으면 전원이 막힌다. 배포 스크립트에 넣어둔다.

---

## 15. 결과 화면 생성 프롬프트 — `web/index.html`

```
web/index.html 을 만들어줘. 의존성 없는 단일 HTML 파일이다.
state/review.json 과 (있으면) drafts/ 의 초안 마크다운을 fetch 해서 렌더한다.

1) 상단 요약 줄
   - "예상 지적 N건 · 근거 있는 질문 M건 · 준비 시간 합계 T분"
   - T가 30분을 넘으면 그 숫자를 강조한다
   - 그 아래 작게: "문진 12문항 중 A개 답변 · 확실한 항목 H개"
   - high_count 가 0이면 상단 전체에 회색 띠를 두르고
     "확실한 근거가 없습니다. 아래 결과를 참고 수준으로 보세요" 를 표시한다

2) 체크리스트 패널
   - rubric 항목을 confidence 순으로 정렬
   - high 는 굵게, medium 은 보통, low 는 40% 투명도로 흐리게
   - 항목을 클릭하면 basis(어느 문항의 어떤 답변에서 나왔는지)가 펼쳐진다

3) mode 가 "review" 일 때 — 좌측 60%
   - 초안을 줄 번호 붙여 표시
   - marks 의 line 에 해당하는 줄에 왼쪽 빨간 세로선
   - 그 줄을 클릭하면 issue 와 fix 가 옆에 펼쳐진다

   mode 가 "guide" 일 때 — 좌측 60%
   - guide 의 section 을 순서대로 카드로 표시
   - 각 카드에 must_include 를 체크박스 목록으로
   - 카드마다 근거 rubric_id 를 작게 표시

4) 우측 40% — 예상 질문 카드
   - q 를 크게, confidence 배지를 옆에
   - speculative: true 인 카드는 40% 투명도 + "근거 없음" 배지
   - answer 를 접었다 펼치는 형태로
   - missing 이 있으면 빨간 점과 함께 목록으로, prep_minutes 를 옆에
   - 카드마다 복사 버튼. "질문 / 답변 / 준비물" 이 한 덩어리로 복사된다

5) 최하단 — 회의 전 준비 목록
   - 모든 missing 을 모아 체크박스 리스트로 다시 보여준다
   - 그 옆에 evidence.improve_by 를 "정확도를 올리려면: 문항 3, 7에 답해보세요" 로 표시
   - 이게 이 화면의 결론이다. 여기까지 스크롤하면 뭘 해야 하는지 알아야 한다

6) logs/trace.jsonl 을 2초마다 다시 읽어 위원 진행 상황을 우측 하단 타임라인으로 표시한다

7) 스타일: 무채색 + 지적 표시에만 빨강. 시스템 폰트, 1px 괘선.
   화면 폭 1400px, 좌우 분할. 그림자와 둥근 모서리 최소화.

8) review.json 이 없으면 "아직 결과가 없습니다" 를 보여준다. 에러 화면 금지.
```

실행:
```bash
python3 -m http.server 8080
```
→ 문진은 `http://localhost:8080/web/intake.html`, 결과는 `http://localhost:8080/web/` **Aha 4.**

`file://` 로 열면 fetch가 CORS로 막힌다. 미리 공지한다.

> **최하단 준비 목록이 이 화면의 진짜 결론이다.** 빨간줄은 보기에 좋고 예상 질문은 읽기에 재밌지만, 사람을 움직이는 건 "회의 전에 이 3개를 준비하라"는 체크리스트다. 강사가 시연할 때 반드시 여기까지 스크롤해서 끝낸다.

---

## 16. HOTL 전환 블록 (2:15–2:28)

문진형이 되면서 이 블록이 훨씬 좋아진다. **입력을 바꿔 재실행하는 게 쉬워졌기 때문이다.**

두 군데만 고친다.

**① `state/approvals.json`**
```json
{ "rubric_approved": true, "mode": "HOTL" }
```

**② `CLAUDE.md` 에 한 단락 추가**
```markdown
## HOTL 모드
mode 가 HOTL 이면 승인 요청 없이 끝까지 진행한다.
단, 아래 중 하나라도 걸리면 즉시 멈추고 사람을 호출한다.
- 필수 문항(1, 10)이 비어 있음
- high 등급 항목이 하나도 없음
- speculative: true 인 질문이 전체의 70% 를 넘음
- 저장물에 성격·성향 추정 표현이 들어감
- 저장물에 이니셜이 아닌 실명이나 회사 고유명사가 남아 있음
- 같은 에이전트 3회 실패
멈출 때는 halts 배열에 사유를 적고 logs/trace.jsonl 에 {"ev":"halt","why":"..."} 를 남긴다.
```

### 실습 방법

수강생은 **터미널을 보지 않는다.** `intake.html` 로 돌아가 답변 3개를 바꾼다.

- 빈도를 하나 올려 본다 (2~3회 → 4회 이상)
- 건너뛴 문항 하나에 답해 본다
- 문항12에 초안을 넣거나 빼서 모드를 바꿔 본다

다시 복사해서 붙여넣고 `/redpen`. 브라우저만 본다. **등급이 올라가고, 준비 목록이 달라지고, 화면 구성이 바뀐다.**

### 마무리 질문 세 개

- "지금 여러분은 뭘 하고 있었습니까?" → 감시. 이게 HOTL
- "HITL과 HOTL 중 뭐가 더 어렵습니까?" → HOTL. 멈추는 조건을 미리 다 적어놔야 하기 때문이다. **자동화의 난이도는 실행이 아니라 정지 조건 설계에 있다**
- "high 등급이 하나도 없으면 멈추라는 조건, 왜 넣었을까요?" → 무인으로 돌기 시작하면 사람이 안 본다. 근거가 약한 결과가 그대로 나가는 게 이 시스템에서 제일 위험하다

세 번째 질문이 2절의 선긋기와 다시 만난다. 수업의 처음과 끝이 같은 이야기로 닫힌다.

---

## 17. 회고 블록 (2:28–2:35) — 검증 루프 예고

마지막 7분에 자기개선 루프를 말로만 예고한다. 구현은 하지 않는다.

> "이번 보고에서 **실제로 나온 질문**을 적어 두세요.
> 예측한 것 중 몇 개가 맞았는지 세어 보세요.
> 그리고 다음 문진에서 빈도 답변을 그 결과대로 고치세요.
> 여러분의 기억을 실제 결과로 교정하는 겁니다. 그 숫자가 올라가면 시스템이 나아진 겁니다."

문진형이라서 이 루프가 더 자연스럽다. 이전 판은 코멘트를 더 모아야 했지만, 지금은 **답변 하나를 고치면 된다.**

하네스 6요소를 한 번 더 짚고 마친다. 규칙 · 역할 · 계약 · 관측 · 게이트 · 기억.

---

## 18. 흔한 사고와 대응

| 증상 | 원인 | 대응 |
|---|---|---|
| `/mcp` 에 redpen-desk 가 안 뜸 | `.mcp.json` 경로/명령 오류 | "MCP 연결이 안 된다, .mcp.json 고쳐줘" 그대로 입력 |
| 게이트가 안 걸림 | `gate.sh` 실행 권한 없음 | `chmod +x .claude/gate.sh` |
| `trace.jsonl` 이 빔 | `jq` 미설치 또는 훅 이벤트명 불일치 | 훅 명령을 `echo` 로 임시 대체 후 재설정 |
| FE가 빈 화면 | `file://` 로 열었음 | `python3 -m http.server 8080` |
| 전 항목이 low 등급 | 문항3 빈도를 전부 "1회"로 답함 | 정상이다. improve_by 문항에 답하고 재실행시킨다 |
| 체크리스트가 2개뿐 | 문항3에서 2개만 선택 | 12개 보기를 다시 천천히 읽게 한다. 재인이 회상보다 잘 된다 |
| 문진관이 등급을 올려서 보고 | `intaker.md` 마지막 금지 절 누락 | weight_rubric 반환값과 대조시킨다 |
| 문진관이 "꼼꼼한 성향" 을 씀 | 금지 절 누락 | **이건 반드시 잡는다.** `save_rubric` 검증도 확인 |
| 예상 질문이 전부 speculative | 체크리스트 항목이 너무 적음 | 문항3 재작성 후 재실행 |
| 답변관이 없는 숫자를 넣음 | `tools` 에 검색이 들어감 | `responder.md` 는 `Read` 만 |
| 가이드 모드인데 초안을 대신 씀 | `marker.md` 가이드 절 누락 | "초안을 대신 써주지 않는다" 줄 확인 |
| 복사 버튼이 비활성 | 문항1 또는 10이 비어 있음 | 필수 2개만 채우면 된다고 안내 |

훅 이벤트명(`SubagentStart`/`SubagentStop`)과 설정 스키마는 Claude Code 버전에 따라 달라진다. **수업 당일 아침에 `/hooks` 로 확인하고 배포본을 확정한다.**

---

## 19. 평가 기준 (수강생 자기 점검)

각 1점, 6점 만점. 4점 이상이면 MVP 완성.

1. `/mcp` 에 내가 만든 서버가 뜬다
2. 체크리스트 항목에 근거 문항 번호와 신뢰등급이 전부 붙어 있다
3. 승인 없이 체크리스트 저장을 시도했을 때 실제로 막혔다
4. 내 초안에 빨간줄이 그어졌다 (또는 작성 가이드가 나왔다)
5. 예상 질문의 신뢰등급이 화면에서 구분된다
6. 최하단 준비 목록에 내가 실제로 준비할 항목이 들어 있다

보너스: 문진 답변을 고쳐 재실행했더니 결과가 달라지는 걸 확인했다.

---

## 20. 확장 (다음 차수용)

- **검증 루프**: 실제로 나온 질문을 기록해 예측 적중률을 차수별로 추적. 문진 답변을 결과로 교정한다
- **리뷰어 복수**: 파트장·팀장·본부장 문진을 각각 두고 보고 단계별로 다른 체크리스트를 태운다
- **문진 자동 확장**: 답변이 부족한 영역에 대해 문진관이 추가 질문 3개를 그 자리에서 생성. **에이전트가 자기 입력을 스스로 보강하는 구조**
- **역방향**: 내가 남의 문서에 단 코멘트를 문진으로 정리해 내 리뷰 기준을 문서화한다
- **A2A**: 답변관의 `missing` 을 편집관에게 되돌려 "이 데이터를 넣으면 지적이 몇 개 사라지는지" 재계산. 수렴 조건 설계가 진짜 난이도
- **관측 고도화**: 훅에 토큰·소요시간을 기록해 "검수 1회 비용" 축 추가
